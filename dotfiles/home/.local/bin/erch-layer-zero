#!/bin/bash
# erch:summary=Sync system packages via Layer Zero (install/remove)
# erch:examples=erch layer-zero | erch layer-zero --dry-run

set -euo pipefail

DOTFILES_DIR="${DOTFILES_DIR:-$HOME/eande/eande-os/dotfiles}"

exec make -C "$DOTFILES_DIR" layer-zero "$@"
