#!/bin/bash
# erch:summary=Deploy dotfiles configs to $HOME via stow
# erch:examples=erch deploy | erch deploy --dry-run

set -euo pipefail

DOTFILES_DIR="${DOTFILES_DIR:-$HOME/eande/eande-os/dotfiles}"

exec make -C "$DOTFILES_DIR" deploy "$@"
