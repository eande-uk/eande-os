#!/bin/bash
# erch:summary=Open PR from current branch to main
# erch:examples=erch pr

set -euo pipefail

DOTFILES_DIR="${DOTFILES_DIR:-$HOME/eande/eande-os/dotfiles}"

exec make -C "$DOTFILES_DIR" pr
