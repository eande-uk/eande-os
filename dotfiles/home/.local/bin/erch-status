#!/bin/bash
# erch:summary=Show repo and deployment status
# erch:examples=erch status

set -euo pipefail

DOTFILES_DIR="${DOTFILES_DIR:-$HOME/eande/eande-os/dotfiles}"

exec make -C "$DOTFILES_DIR" status
